1 可变参数
* kotlin中方法形参使用vararg关键词定义可变参数。
* 一个方法中只能有一个可变参数。java中可变参数只能在参数列表最后位置，kotlin没有这个限制。
* 数组使用展开运算符*赋值可变参数。
* 可变参数实际是使用数组实现的。

2 中辍调用
* 使用infix关键词修饰的成员函数或者扩展函数称为中辍函数。
* 中辍函数有且只有一个函数。
* a.to(b)使用中辍调用 a to b。

3 结构声明
* kotlin可以对data类使用结构声明，一次性将其所有属性声明。
* val(a,b) = 1 to 2